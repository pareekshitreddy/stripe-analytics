version: 2
sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
        description: Has all the transactions associated with the orders
        columns:
          - payment_id: primary key